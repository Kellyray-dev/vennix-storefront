# runtime data — non-commerce leads captured by the storefront
# (newsletter, contact, back-in-stock, review submissions)
data/leads.json
data/leads.*.tmp

# build output
dist/

# dependencies + local tooling
node_modules/
.tmp/
*.log

# editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# generated per-machine by `npm run brand:theme` (holds your store handle)
shopify-theme/shopify.theme.toml

# local env
.env
.env.local
brand.json
